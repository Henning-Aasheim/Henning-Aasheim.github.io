export interface ArticlePost {
    id: string
    title: string
    date: string
}

export interface ArticleItem {
    image: string
    category: string
    title: string
    author: string
    date: string
}